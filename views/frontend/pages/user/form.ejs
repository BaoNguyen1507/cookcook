<!-- Page -->
<div class="row">
  <div class="col-xxl-12 col-xl-12 col-lg-12">
    <div class="card">
      <div class="card-body"> 
        <!-- <%if (manner == 'add') {%>
        <h4 class="card-title"><%= __('Tạo mới nhân viên') %></h4>
        <% }else {%>
        <h4 class="card-title"><%= __('Chỉnh sửa nhân viên') %></h4>
        <%}%>   -->
        <%- partial('./_form') %>
      </div>
    </div>
  </div>
</div> 
<% block('localScripts', '<script src="/vendors/sweetalert/sweetalert.min.js"></script>') %>
<% block('localScripts', '<script src="/js/backend/user/form.js"></script>') %>


